import { FC } from 'react';
import styles from './../../../styles/postCard.module.css';

interface VoteCountProps {}
export const VoteCount: FC = () => {
  return <div className={styles.vote__count}>123</div>;
};
